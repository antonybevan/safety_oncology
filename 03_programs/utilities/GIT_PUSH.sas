/* GIT_PUSH.sas: Push Instructions for SAS OnDemand */
%put NOTE: SAS OnDemand cannot push directly (No Git CLI/PROC GIT).;
%put NOTE: 1. Right-click folder -> Download as Zip.;
%put NOTE: 2. Extract locally and push using Git.;
